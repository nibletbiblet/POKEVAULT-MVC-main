<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link href="/theme.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Admin - Trades</title>
  <style>
    body { background:#0d0f12; color:#fff; font-family:'Montserrat',system-ui,-apple-system,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif; }
    body::before { content:''; position:fixed; inset:0; background:linear-gradient(180deg, rgba(7,7,8,0.82) 0%, rgba(10,12,15,0.88) 40%, rgba(10,12,15,0.92) 100%); z-index:-1; }
    .custom-navbar { background-color:transparent !important; padding-top:1.05rem; padding-bottom:1.05rem; min-height:80px; z-index:1030; box-shadow:none; }
    .custom-navbar .navbar-brand { font-weight:700; letter-spacing:-0.01em; }
    .brand-logo { font-family:'Montserrat',system-ui,sans-serif; display:inline-flex; flex-direction:column; text-align:center; text-decoration:none; line-height:1.05; padding-left:0.1rem; }
    .brand-main { font-size:1.45rem; font-weight:700; letter-spacing:0.33em; text-transform:uppercase; color:#ffffff; }
    .brand-sub { font-size:0.68rem; font-weight:500; letter-spacing:0.28em; text-transform:uppercase; color:rgba(255,255,255,0.75); margin-top:0.15rem; }
    .card-frosted { background:rgba(255,255,255,0.05); border:1px solid rgba(255,255,255,0.08); border-radius:1rem; -webkit-backdrop-filter:blur(14px); backdrop-filter:blur(14px); box-shadow:0 18px 48px rgba(0,0,0,0.55); padding:1.4rem; }
    .table-dark th, .table-dark td { border-color: rgba(255,255,255,0.08); }
    .table-dark tbody tr { background: rgba(8, 24, 48, 0.62) !important; }
    .table-dark tbody tr:hover { background: rgba(12, 32, 64, 0.75) !important; }
    .status-pill { display:inline-flex; align-items:center; justify-content:center; border-radius:999px; padding:0.3rem 0.8rem; font-weight:800; font-size:0.78rem; letter-spacing:0.02em; }
    .status-open { background: rgba(59,130,246,0.15); border:1px solid rgba(59,130,246,0.45); color:#bfdbfe; }
    .status-pending_initiator { background: rgba(234,179,8,0.16); border:1px solid rgba(234,179,8,0.45); color:#fde68a; }
    .status-accepted { background: rgba(16,185,129,0.18); border:1px solid rgba(16,185,129,0.45); color:#a7f3d0; }
    .status-declined { background: rgba(239,68,68,0.18); border:1px solid rgba(239,68,68,0.5); color:#fecdd3; }
    .status-cancelled { background: rgba(107,114,128,0.18); border:1px solid rgba(107,114,128,0.5); color:#e5e7eb; }
    .status-unknown { background: rgba(255,255,255,0.12); border:1px solid rgba(255,255,255,0.2); color:#fff; }
    footer.site-footer { background:#0b0c0d; color:#fff; padding:2.5rem 0; border-top:1px solid rgba(255,255,255,0.03); }
    a, .nav-link { color: rgba(255,255,255,0.95); }
    .stat-pill { background:rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.12); border-radius:999px; padding:0.35rem 0.8rem; font-weight:700; color:#fff; letter-spacing:0.01em; }
  </style>
</head>
<body class="theme-gradient d-flex flex-column min-vh-100">
  <nav class="navbar navbar-expand-sm navbar-dark bg-dark custom-navbar">
    <div class="container-fluid">
      <a class="navbar-brand brand-logo" href="/">
        <span class="brand-main">PokeVault</span>
        <span class="brand-sub">SINGAPORE</span>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar"><span class="navbar-toggler-icon"></span></button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item d-flex align-items-center me-3"><span class="text-light small">Signed in as <strong><%= user.username %></strong></span></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/admin/dashboard">Dashboard</a></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/admin/audit-log">Audit Log</a></li>
          <li class="nav-item ms-2"><a class="btn btn-outline-light btn-sm btn-cta btn-minimal" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container flex-grow-1 py-4">
    <div class="card-frosted mb-3">
      <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-2">
        <div>
          <h4 class="fw-bold mb-1">All Trades</h4>
          <small class="text-white-50">Monitor every collector trade and its status.</small>
        </div>
        <div class="d-flex flex-wrap gap-2">
          <span class="stat-pill">Open: <%= statusCounts.open || 0 %></span>
          <span class="stat-pill">Pending Initiator: <%= statusCounts.pending_initiator || 0 %></span>
          <span class="stat-pill">Accepted: <%= statusCounts.accepted || 0 %></span>
          <span class="stat-pill">Declined: <%= statusCounts.declined || 0 %></span>
          <span class="stat-pill">Cancelled: <%= statusCounts.cancelled || 0 %></span>
        </div>
      </div>

      <% if (!trades || !trades.length) { %>
        <p class="text-white-50 mb-0">No trades posted yet.</p>
      <% } else { %>
        <div class="table-responsive">
          <table class="table table-dark table-hover align-middle mb-0">
            <thead>
              <tr>
                <th scope="col">Trade</th>
                <th scope="col">Owner</th>
                <th scope="col">Owner Card</th>
                <th scope="col">Responder</th>
                <th scope="col">Responder Card</th>
                <th scope="col">Status</th>
                <th scope="col">Updated</th>
              </tr>
            </thead>
            <tbody>
              <% trades.forEach(t => { %>
                <% const updated = t.updated_at ? new Date(t.updated_at) : null; %>
                <% const responderName = t.responderUsername || (t.responder_id ? ('User #' + t.responder_id) : 'Awaiting offer'); %>
                <% const responderCard = t.responder_product_id ? (t.responderProductName || ('Card #' + t.responder_product_id)) : 'No offer yet'; %>
                <tr>
                  <td class="fw-semibold">#<%= t.id %></td>
                  <td>
                    <div><%= t.initiatorUsername || ('User #' + t.initiator_id) %></div>
                    <div class="text-white-50 small">User ID: <%= t.initiator_id %></div>
                  </td>
                  <td><%= t.initiatorProductName || ('Card #' + t.initiator_product_id) %></td>
                  <td>
                    <div><%= responderName %></div>
                    <% if (t.responder_id) { %>
                      <div class="text-white-50 small">User ID: <%= t.responder_id %></div>
                    <% } %>
                  </td>
                  <td><%= responderCard %></td>
                  <td>
                    <span class="status-pill status-<%= t.status || 'unknown' %>"><%= (t.status || 'unknown').replace('_',' ') %></span>
                  </td>
                  <td><%= updated ? updated.toLocaleString() : '' %></td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
      <% } %>
    </div>
  </main>
  <footer class="site-footer mt-auto">
    <div class="container text-center">
      <p class="mb-1">Connecting Pokemon card collectors worldwide - shop, sell, and trade in a safe and growing community.</p>
      <small class="small">&copy; <%= new Date().getFullYear() %> PokeVault. All rights reserved.</small>
    </div>
  </footer>
</body>
</html>
